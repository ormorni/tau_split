# Models

A directory containing models for testing the data.

The B-cell receptor network and the FceRI network can be downloaded from `https://www.cosbi.eu/prototypes/rssa`. For ease of use, the script `scripts/download_data.py` will download and unpack them to this directory.
